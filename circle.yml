machine:
  environment:
    NODE_ENV: test
database:
  override:
    - echo "ubuntu" | npm run dev-setup
deployment:
  demo:
    branch: master
    heroku:
      appname: fj16-procurement-demo
test:
  pre:
    - echo "ubuntu" | ./scripts/create-ci-dbadmin.sh

