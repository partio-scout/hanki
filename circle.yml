machine:
  environment:
    NODE_ENV: test
  node:
    version: v0.12.0
database:
  override:
    - npm run dev-setup
deployment:
  demo:
    branch: demo
    heroku:
      appname: fj16-procurement-demo
dependencies:
  post:
    - java -jar e2e-tests/libs/selenium-server-standalone-2.47.1.jar:
        background: true

