machine:
  environment:
    NODE_ENV: test
database:
  override:
    - npm run dev-setup
deployment:
  demo:
    branch: demo
    heroku:
      appname: fj16-procurement-demo
dependencies:
  post:
    - npm run selenium:
        background: true

